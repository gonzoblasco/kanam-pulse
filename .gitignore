# Project .gitignore
# .knowledge/ IS committed here. It exposes how we work to contributors.
# Rule: NEVER document sensitive information (tokens, internal decisions,
# personal notes, credentials, internal URLs, billing info, etc.).

# Node
node_modules/
dist/
.next/
.turbo/
coverage/

# Env
.env
.env.local
.env.*.local

# OS
.DS_Store
