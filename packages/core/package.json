{
  "name": "@kanam-pulse/core",
  "version": "0.1.0",
  "description": "Health-check engine: checks, metrics collectors, fix planners (zero UI deps)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  }
}
