// @kanam-pulse/core
// Inherited engine goes here: checks (disk, memory, cpu, security, network),
// metrics collectors, fix planners. Isolated by design - one failing check
// never kills the battery.
export const ENGINE_PLACEHOLDER = true;
