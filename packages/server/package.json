{
  "name": "@kanam-pulse/server",
  "version": "0.1.0",
  "description": "Local HTTP API + SSE stream (binds 127.0.0.1 only)",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@kanam-pulse/core": "*",
    "fastify": "^5"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  }
}
